export const isNameValid = (val) => {
	return val.length > 3 ? true : false;
};
export const isTeamValid = (val) => {
	return val ? true : false;
};
export const isPositionValid = (val) => {
	return val ? true : false;
};
export const isImageUrlValid = (val) => {
	return true;
};
